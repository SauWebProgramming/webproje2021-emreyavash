@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer localizer
@model RentAndDrive.Models.User
@{
    ViewData["Title"] = "Giriş Yap";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="formContainer">
    <div class="formController">
        <form class="form-group" action="Index" asp-controller="User" method="post" enctype="multipart/form-data">

            <label>Email</label>
            @Html.TextBoxFor(x => x.Email, new { @class = "form-control", type = "email", placeholder = "email@mail.com" })
            <br />
            <label>@localizer["Password"]</label>
            @Html.TextBoxFor(x => x.Password, new { @class = "form-control", type = "password" })
            <br />
            @ViewBag.mesaj
            <br />
            <div class="btnContactContainer">
                <button class="btnContact">@localizer["Login"]</button>
            </div>
        </form>
    </div>
</div>